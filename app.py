import streamlit as st

st.title("📧 Email Intelligence")

st.write("Click below to fetch emails")

if st.button("Fetch Emails"):
    st.success("Fetching emails...")

    # TEMP test (replace later)
    st.write("This is where your fetcher will run")